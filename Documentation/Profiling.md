# Profiling with Chrome

Under `chrome://tracing/`open the one of the files located in `Saved/GoogleChrome`.
To profile Jafg, the runtime of the engine has to be launched with the `-AllowProfiling` flag and the compile time option `WITH_STATS` has to be enabled (which is the default for any development build).
